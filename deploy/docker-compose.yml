services:
  mcp-server:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: pdf-generator-mcp-server
    ports:
      - "3001:3000"
    env_file:
      - ../.env
    restart: unless-stopped