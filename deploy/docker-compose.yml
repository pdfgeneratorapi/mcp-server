services:
  mcp-server:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: ar-mcp-server
    ports:
      - "3001:3000"
    env_file:
      - ../.env
    restart: unless-stopped