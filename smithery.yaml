startCommand:
  type: http
  configSchema:
    type: object
    description: Connect to the PDF Generator API MCP server. Authentication is handled via the Authorization header with a JWT bearer token.
    properties:
      bearerTokenJwt:
        type: string
        title: Bearer Token (JWT)
        description: JWT bearer token for PDF Generator API authentication
    required: []
  exampleConfig: {}
